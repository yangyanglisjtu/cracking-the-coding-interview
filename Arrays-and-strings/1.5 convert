#include<iostream>
#include<string>
using namespace std;
int main(){
    string s="I am a string";
    string res_s;
    for(auto c:s){
       if(c==' '){
         res_s+="%20";
       }else{
         res_s+=c;
       }
    }
    cout<<res_s<<endl;
    cin.get();
}
